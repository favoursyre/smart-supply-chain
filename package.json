{
  "dependencies": {
    "bignumber.js": "^9.0.2",
    "dotenv": "^16.0.1",
    "ethers": "^5.6.8",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base-path . -o . contract.sol"
  }
}
